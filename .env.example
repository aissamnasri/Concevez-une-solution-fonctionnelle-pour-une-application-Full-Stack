# Copiez ce fichier en .env pour surcharger les valeurs par defaut du docker-compose.
# Le fichier .env n'est pas versionne (voir .gitignore).
POSTGRES_DB=ycyw_chat
POSTGRES_USER=ycyw
POSTGRES_PASSWORD=ycyw-local-dev
